/*
import React from 'react';
import { View } from 'react-native';
import { propTypes } from './index';

export function {{ properCase name }}({ children, style, ...rest }) {
  return (
   <View {...rest} style={style}>
      {children}
   </View>
  );
}

{{ properCase name }}.propTypes = propTypes;
*/
