import React from 'react';

import Text from '../index';

export const name = 'styled';
export const description = 'This is a styled text element.';

export default function () {
  return (
    <Text color="#bb4444" size={30}>This is a styled text element.</Text>
  );
}

