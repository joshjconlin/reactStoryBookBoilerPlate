import React from 'react';

import Text from '../index';

export const name = 'Default';
export const description = 'This is a simple inline text element with no styling';

export default function () {
  return (
    <Text>This is a simple text element.</Text>
  );
}

